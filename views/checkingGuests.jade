extends layout

block content
  div.reader-container
    div#reader
  script(type="text/javascript").
    $('#reader').html5_qrcode(function(data){
      $.post( "/checkingGuests", { str: data }).done(function(data) {
        console.log( "second success: "+ data.redirect )
        window.location.replace(data.redirect);
      });

    },
    function(error){
      console.log(error)
    }, function(videoError){
      console.log(videoError)
    });
